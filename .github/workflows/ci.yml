name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: [develop, main]

jobs:
  ci-fast:
    # Reemplazar MarbiFox por tu usuario/organización real de GitHub.
    uses: MarbiFox/.github/.github/workflows/ci-fast.yml@main
