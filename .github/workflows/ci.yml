name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: [develop, main]

jobs:
  ci-fast:
    # Reemplazar TU-USUARIO por tu usuario/organización real de GitHub.
    uses: TU-USUARIO/.github/.github/workflows/ci-fast.yml@main
