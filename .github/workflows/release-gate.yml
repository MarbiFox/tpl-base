name: Release gate

on:
  pull_request:
    branches: [main]

jobs:
  release-gate:
    # Reemplazar TU-USUARIO por tu usuario/organización real de GitHub.
    uses: TU-USUARIO/.github/.github/workflows/release-gate.yml@main
